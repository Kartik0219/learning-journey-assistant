[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Sensible baseline: errors/warnings, import sorting, and unused-var checks.
select = ["E", "F", "I", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
